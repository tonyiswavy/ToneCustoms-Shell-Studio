# Shell Studio tests
Test coverage targets project serialization, snapping, geometry generation, DDS validation, collision generation, dependency detection, export validation, and FiveM resource construction. End-to-end GTA export tests require Blender + compatible Sollumz + CodeWalker + a FiveM test environment.
