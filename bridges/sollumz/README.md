# Sollumz adapters
Shell Studio detects the user's installed Sollumz copy/version before GTA export. Version-specific adapters belong here. An adapter must never report success merely because Blender produced a .blend file; it must verify that the requested GTA assets were actually emitted and then pass them through Shell Studio's output verifier.
